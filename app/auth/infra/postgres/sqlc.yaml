version: "2"
sql:
  - engine: "postgresql"
    schema: "schema/migrations"
    queries: "schema/queries"
    gen:
      go:
        package: "sqlcgen"
        out: "sqlcgen"
        sql_package: "database/sql"
        emit_interface: true
        emit_json_tags: false
