version: "2"
sql:
  - engine: "postgresql"
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "sqlcgen"
        out: "infra/postgres/sqlcgen"
        sql_package: "database/sql"
        emit_interface: true
        emit_json_tags: false
