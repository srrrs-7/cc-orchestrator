# `go build ./cmd/migrator`, run manually from this directory, writes
# an executable named after the package (migrator) here. `make build`
# (`go build ./...`) itself no longer produces this file (2026-07-10
# DDD layering refactor: this module now has multiple packages --
# cmd/migrator, domain/migration, service, infra/postgres, infra/goose
# -- like app/api and app/auth, so `go build ./...` only type-checks
# and discards output). Never commit it.
/migrator
